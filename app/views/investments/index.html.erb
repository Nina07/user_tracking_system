<!-- <h1>Listing investments</h1>

<table>
  <thead>
    <tr>
      <th>Month</th>
      <th>Amount</th>
      <th>Total inv</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @investments.each do |investment| %>
      <tr>
        <td><%= investment.month %></td>
        <td><%= investment.amount %></td>
        <td><%= investment.total_inv %></td>
        <td><%= link_to 'Show', investment %></td>
        <td><%= link_to 'Edit', edit_investment_path(investment) %></td>
        <td><%= link_to 'Destroy', investment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Investment', new_investment_path %> -->

<br>
<h2>Here are your Investment Details</h2><br>
<fieldset>

<p>
  <h3><strong>Your current account balance is:</strong>
<%= @user.total_amount %>
</p></h3>

<p> 
  <p id="notice"><%= notice %></p>
   <%= form_tag(:action=>'edit') do %>
   <%= label_tag(:amount, "Amount u want to invest:  ") %>
   <%= text_field_tag(:amount) %><br>

   <br>
    <center><%= submit_tag 'Edit amount' %></center><br>
    <center><%= submit_tag "Show current Investments",controller: "investments", action: "show", id: @investment.id %></center><br>
    <%#= button_to "Show my current investements", investment_path, id: @investment.id %>
    <center><%= link_to "Back", controller: "users" , action: "show" , id: @user.id %>

    <%end%>
</fieldset>